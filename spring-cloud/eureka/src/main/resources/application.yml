spring:
  application:
    name: myCloudRegister
  profiles:
    active: dev

eureka:
  client:
    fetch-registry: false
    register-with-eureka: false
  server:
    disable-delta: true

endpoints:
  shutdown:
    enabled: true

